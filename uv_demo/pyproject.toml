[project]
name = "uv-demo"
version = "0.1.0"
description = "A minimal demo of uv for Python project management"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
